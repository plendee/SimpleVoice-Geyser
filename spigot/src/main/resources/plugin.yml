name: SimpleVoice-Geyser
version: ${plugin_version}
main: io.github.theodoremeyer.simplevoicegeyser.spigotmc.SvgPlugin
api-version: ${spigot_version}
description: Plugin to allow Bedrock clients to join Simple Voice Chat.
author: TheodoreMeyer
website: https://theodoremeyer.github.io
depend: [voicechat]
softdepend: [Geyser-Spigot, floodgate]
prefix: SVG
folia-supported: true

commands:
  svg:
    description: Main SimpleVoiceGeyser command
    usage: /svg help

permissions:
  svg.admin:
    description: Allows player to use all SimpleVoiceGeyser admin commands and features
    default: op

  svg.vc:
    description: Allows player to use voice chat features from browser
    default: op
    children:
      svg.vc.join: true
      svg.vc.group: true

  svg.vc.join:
    description: Allows player to join voice chat from browser
    default: true

  svg.vc.group:
    description: Allows player to use voice chat group creation
    default: true
    children:
      svg.vc.group.create: true
      svg.vc.group.type: true
      svg.vc.group.type.isolated: true

  svg.vc.group.create:
    description: "Allows the player to create voice chat groups"
    default: true

  svg.vc.group.type:
    description: "Allows the player to specify the type of the group"
    default: op

  svg.vc.group.type.isolated:
    description: "Allows the player to create isolated groups"
    default: op

  svg.vc.group.setpersistant:
    description: "Allows the player to set persistent groups"
    default: op
